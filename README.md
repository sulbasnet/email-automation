# Python Email Automation

A simple Python script to send automated emails to multiple recipients using the Gmail SMTP server.  
Built with `smtplib`, `pandas`, and `email.mime`.

---

## ⚙️ Features
- Send multiple emails automatically
- Read recipient names & emails from a CSV file
- Supports personalized messages with `{name}`
- Uses secure Gmail App Password for authentication

---

## 🧠 Requirements
```bash
pip install pandas
